# v1.4 架构升级 - 需求清单

**对比时间**: 2026-03-10 16:30  
**当前版本**: v1.3.0  
**目标版本**: v1.4.0 (SQLite + Repository 层)

---

## ✅ 已有文件 (无需修改)

| 文件 | 状态 | 说明 |
|------|------|------|
| `database/db.py` | ✅ 90% | 连接配置基本正确，需升级到 SQLAlchemy 2.0 |
| `database/models.py` | ✅ 80% | 模型基本正确，需添加新字段和索引 |
| `database/__init__.py` | ✅ OK | 导入文件 |
| `database/cache.py` | ✅ OK | 缓存模块 |
| `routes/*.py` | ✅ OK | 路由层 |
| `services/*.py` | ✅ OK | 服务层 |
| `platform/*.py` | ✅ OK | 适配器层 |
| `tools/*.py` | ✅ OK | 工具层 |
| `auth/*.py` | ✅ OK | JWT 认证 |

---

## ❌ 缺失文件 (需要创建)

### 高优先级 🔴

| 文件 | 说明 | 预计工时 |
|------|------|----------|
| `repositories/__init__.py` | Repository 层导入 | 5 分钟 |
| `repositories/base.py` | Repository 基类 | 30 分钟 |
| `repositories/user_repo.py` | 用户仓库 | 30 分钟 |
| `repositories/job_repo.py` | 职位仓库 | 30 分钟 |
| `repositories/match_repo.py` | 匹配仓库 | 30 分钟 |
| `repositories/application_repo.py` | 投递仓库 | 30 分钟 |
| `alembic.ini` | Alembic 配置 | 15 分钟 |
| `migrations/env.py` | Alembic 环境配置 | 15 分钟 |
| `migrations/versions/001_initial.py` | 初始迁移脚本 | 30 分钟 |

### 中优先级 🟡

| 文件 | 说明 | 预计工时 |
|------|------|----------|
| `database/session.py` | 会话管理 (可选) | 15 分钟 |
| `services/user_service.py` | 用户服务 (调用 Repository) | 30 分钟 |
| `services/job_service.py` | 职位服务 (调用 Repository) | 30 分钟 |

### 低优先级 🟢

| 文件 | 说明 | 预计工时 |
|------|------|----------|
| `tests/test_database.py` | 数据库测试 | 1 小时 |
| `tests/test_repositories.py` | Repository 测试 | 1 小时 |
| `migrate.py` | 旧数据迁移脚本 | 30 分钟 |

---

## 🔧 需要修改的文件

### 1. `database/db.py` (小改)

**当前**:
```python
from sqlalchemy.ext.declarative import declarative_base
Base = declarative_base()
```

**修改为 SQLAlchemy 2.0**:
```python
from sqlalchemy.orm import DeclarativeBase
class Base(DeclarativeBase):
    pass
```

**预计工时**: 10 分钟

---

### 2. `database/models.py` (中改)

**需要添加**:

#### Jobs 表新增字段
```python
source_platform_id = Column(String(100))  # 平台职位 ID
```

#### Jobs 表新增索引
```python
__table_args__ = (
    Index('idx_jobs_unique', 'platform', 'source_platform_id', unique=True),
    Index('idx_jobs_title', 'title'),
)
```

#### Matches 表新增字段
```python
match_vector = Column(JSON)  # 匹配向量缓存
```

#### Applications 表新增字段
```python
platform_status = Column(String(50))  # 平台状态
```

**预计工时**: 30 分钟

---

### 3. `routes/*.py` (小改)

**当前**:
```python
@router.get("/users/{user_id}")
def get_user(user_id: int, db: Session = Depends(get_db)):
    # 直接查询数据库
    user = db.query(User).filter(User.id == user_id).first()
```

**修改为**:
```python
@router.get("/users/{user_id}")
def get_user(user_id: int, db: Session = Depends(get_db)):
    # 通过 Repository 查询
    repo = UserRepository(db)
    user = repo.get_by_id(user_id)
```

**预计工时**: 1 小时 (4 个路由文件)

---

### 4. `services/*.py` (中改)

**当前**:
```python
class UserService:
    def __init__(self, db: Session):
        self.db = db
    
    def create_user(self, data: dict):
        user = User(**data)
        self.db.add(user)
        self.db.commit()
```

**修改为**:
```python
class UserService:
    def __init__(self, db: Session):
        self.user_repo = UserRepository(db)
    
    def create_user(self, data: dict):
        return self.user_repo.create(data)
```

**预计工时**: 1 小时 (4 个服务文件)

---

## 📦 需要安装的依赖

```bash
pip install alembic
```

**预计工时**: 5 分钟

---

## 📋 任务清单

### 阶段 1: Repository Layer (2 小时)

- [ ] 创建 `repositories/__init__.py`
- [ ] 创建 `repositories/base.py` (基类)
- [ ] 创建 `repositories/user_repo.py`
- [ ] 创建 `repositories/job_repo.py`
- [ ] 创建 `repositories/match_repo.py`
- [ ] 创建 `repositories/application_repo.py`

### 阶段 2: 数据库迁移 (1 小时)

- [ ] 安装 Alembic
- [ ] 初始化 Alembic (`alembic init migrations`)
- [ ] 配置 `alembic.ini`
- [ ] 创建初始迁移脚本
- [ ] 执行迁移 (`alembic upgrade head`)

### 阶段 3: 模型升级 (30 分钟)

- [ ] 升级 `database/db.py` 到 SQLAlchemy 2.0
- [ ] 升级 `database/models.py` (添加字段和索引)
- [ ] 测试模型 (创建表)

### 阶段 4: 服务层集成 (1.5 小时)

- [ ] 更新 `services/user_service.py`
- [ ] 更新 `services/job_service.py`
- [ ] 更新 `services/match_service.py`
- [ ] 更新 `services/application_service.py`

### 阶段 5: 路由层集成 (1 小时)

- [ ] 更新 `routes/users.py`
- [ ] 更新 `routes/jobs.py`
- [ ] 更新 `routes/matches.py`
- [ ] 更新 `routes/applications.py`

### 阶段 6: 测试 (1 小时)

- [ ] 创建 `tests/test_database.py`
- [ ] 创建 `tests/test_repositories.py`
- [ ] 运行集成测试
- [ ] 修复问题

---

## ⏱️ 总工时估算

| 阶段 | 预计时间 |
|------|----------|
| 阶段 1: Repository Layer | 2 小时 |
| 阶段 2: 数据库迁移 | 1 小时 |
| 阶段 3: 模型升级 | 30 分钟 |
| 阶段 4: 服务层集成 | 1.5 小时 |
| 阶段 5: 路由层集成 | 1 小时 |
| 阶段 6: 测试 | 1 小时 |

**总计**: **7.5 小时**

---

## 🎯 执行顺序

1. **先创建 Repository Layer** (基础架构)
2. **再配置 Alembic** (数据库迁移)
3. **然后升级 Models** (表结构)
4. **接着集成 Services** (业务逻辑)
5. **最后更新 Routes** (API 端点)
6. **编写测试** (质量保证)

---

## 📝 需要的支持

### 需要庆确认的事项

1. **是否现在开始执行？** (预计 7.5 小时)
2. **是否需要分阶段执行？** (例如今天完成阶段 1-2)
3. **是否需要保留旧内存存储作为备份？** (双写模式)

### 不需要额外资源

- ✅ SQLAlchemy 2.0 已在 requirements.txt 中
- ✅ FastAPI 已配置
- ✅ 数据库路径已配置 (`data/qing_agent.db`)

---

**准备就绪!** 等庆确认后开始执行 🚀
