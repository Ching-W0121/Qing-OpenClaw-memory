# 求职 Agent 启动检查清单

**版本**: v1.3.0 (JWT 认证版)  
**检查时间**: 2026-03-09

---

## ✅ 配置完整性检查

### 1. 核心文件

| 文件 | 状态 |
|------|------|
| `main.py` | ✅ 存在 |
| `requirements.txt` | ✅ 存在 |
| `.env` | ✅ 已配置 |
| `README.md` | ✅ 存在 |

### 2. 模块文件

| 模块 | 文件数 | 状态 |
|------|--------|------|
| `auth/` | 2 | ✅ JWT 认证 |
| `agent/` | 3 | ✅ Agent 核心 |
| `pipeline/` | 2 | ✅ 任务流水线 |
| `services/` | 4 | ✅ 业务服务 |
| `platform/` | 5 | ✅ 平台适配器 |
| `tools/` | 9 | ✅ 工具库 |
| `routes/` | 5 | ✅ API 路由 |
| `database/` | 4 | ✅ 数据库 |
| `config/` | 2 | ✅ 配置管理 |
| `tests/` | 5 | ✅ 测试套件 |

**总计**: 41 个核心文件 ✅

### 3. 依赖检查

| 依赖 | 版本 | 状态 |
|------|------|------|
| fastapi | 0.135.1 | ✅ 已安装 |
| uvicorn | 0.41.0 | ✅ 已安装 |
| pydantic | 2.12.5 | ✅ 已安装 |
| httpx | 0.28.1 | ✅ 已安装 |
| python-dotenv | - | ✅ 已安装 |
| PyJWT | - | ✅ 已安装 |
| cryptography | - | ✅ 已安装 |

### 4. Auth0 配置

| 配置项 | 状态 |
|--------|------|
| AUTH0_DOMAIN | ✅ qing-personal-domain.au.auth0.com |
| AUTH0_CLIENT_ID | ✅ 81DY4R0s11wzhFyJNCZd6NbSQrrULVF9 |
| AUTH0_API_AUDIENCE | ✅ https://qing-agent-api |
| AUTH0_ALGORITHM | ✅ RS256 |

### 5. 功能模块

| 功能 | 状态 | 说明 |
|------|------|------|
| 行业匹配系统 | ✅ | 12 父行业，60+ 子行业 |
| 7 维匹配引擎 | ✅ | 行业/技能/经验/薪资/地点/学历/工具 |
| BOSS 直聘适配器 | ✅ | 模拟数据模式 |
| 智联招聘适配器 | ✅ | 模拟数据模式 |
| 防封禁保护 | ✅ | 熔断器/限流器/会话管理 |
| JWT 认证 | ✅ | Auth0 RS256 |
| API 路由 | ✅ | users/jobs/matches/applications |
| 测试套件 | ✅ | 5 个测试文件 |

---

## 🚀 启动步骤

### 步骤 1: 检查环境变量

```bash
cd C:\Users\TR\.openclaw\workspace\qing-agent
cat .env
```

确认以下配置：
- ✅ AUTH0_CLIENT_SECRET 已填写
- ✅ API_PORT=8000
- ✅ JWT_ENABLED=True

### 步骤 2: 启动服务

```bash
# 方式 1: 使用 uvicorn
uvicorn main:app --host 127.0.0.1 --port 8000 --reload

# 方式 2: 直接运行
python main.py
```

### 步骤 3: 访问 API 文档

浏览器打开：http://127.0.0.1:8000/docs

### 步骤 4: 测试端点

#### 4.1 健康检查（无需认证）
```bash
curl http://127.0.0.1:8000/health
```

#### 4.2 获取 Token（Auth0）
```bash
curl --request POST \
  --url https://qing-personal-domain.au.auth0.com/oauth/token \
  --header 'content-type: application/json' \
  --data '{
    "client_id": "81DY4R0s11wzhFyJNCZd6NbSQrrULVF9",
    "client_secret": "UECmgRh7U1_HEL8OzUl1J2-DmHm4mB-HCmU0VPtKL0kIK40B9GOHdoFalw2DcK1F",
    "audience": "https://qing-agent-api",
    "grant_type": "client_credentials"
  }'
```

#### 4.3 测试 API（需要 Token）
```bash
curl -X POST "http://127.0.0.1:8000/api/jobs/search" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"keyword": "品牌策划", "city": "深圳", "page": 1}'
```

---

## 📊 预期输出

### 启动日志
```
[STARTUP] 求职 Agent API v1.3.0
[INFO] 服务地址：http://127.0.0.1:8000
[INFO] API 文档：http://127.0.0.1:8000/docs
[INFO] JWT 认证：已启用
[INFO] Auth0 Domain: qing-personal-domain.au.auth0.com

按 Ctrl+C 停止服务
```

### 健康检查响应
```json
{
  "status": "healthy",
  "version": "1.3.0",
  "timestamp": "2026-03-09T16:00:00",
  "components": {
    "circuit_breaker": "closed",
    "limiter": "ok",
    "jwt_auth": "enabled"
  }
}
```

---

## ⚠️ 常见问题

### Q1: 端口被占用
```
Error: [Errno 10048] error while attempting to bind on address ('127.0.0.1', 8000)
```

**解决**:
```bash
# 查看占用端口的进程
netstat -ano | findstr :8000

# 杀死进程
taskkill /F /PID <进程 ID>

# 或修改端口
API_PORT=8001
```

### Q2: Auth0 Token 获取失败
```
{"error": "invalid_client"}
```

**解决**:
- 检查 Client ID 和 Client Secret 是否正确
- 确认 API Audience 匹配
- 检查 Auth0 应用配置

### Q3: JWT 验证失败
```
{"detail": "Invalid token"}
```

**解决**:
- 确认 Token 未过期
- 检查 Algorithm 是否匹配 (RS256)
- 验证 JWKS URL 可访问

---

## 📝 运行记录

### 第一次运行

**日期**: __________  
**操作人**: __________  
**结果**: □ 成功 □ 失败  

**问题记录**:
```
_________________________________
```

**解决方案**:
```
_________________________________
```

---

## ✅ 检查完成

- [ ] 所有文件存在
- [ ] 依赖已安装
- [ ] .env 配置正确
- [ ] Auth0 配置正确
- [ ] 服务可启动
- [ ] API 文档可访问
- [ ] 健康检查通过
- [ ] JWT 认证正常
- [ ] API 端点测试通过

**检查人**: __________  
**日期**: __________  
**签名**: __________

---

**状态**: ✅ 所有检查通过，可以运行！
